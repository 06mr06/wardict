workflows:
  ios-debug:
    name: iOS Debug Build
    instance_type: mac_mini_m1
    environment:
      vars:
        GOOGLE_SERVICES_PLIST: ENCRYPTED[!]
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Decode Firebase config
        script: |
          echo $GOOGLE_SERVICES_PLIST | base64 --decode > $CM_BUILD_DIR/ios/Runner/GoogleService-Info.plist
      - name: Get packages
        script: flutter pub get
      - name: Recreate iOS Configuration
        script: |
          # Git reposundan sildiğimiz Podfile ve iOS bağımlılık şemalarını 
          # bulut makinesinde sıfırdan, en güncel ve hatasız haliyle ürettiriyoruz:
          flutter create . --platforms=ios
      - name: Build debug (Flutter will run pod install automatically)
        script: |
          flutter build ios --debug --no-codesign
    artifacts:
      - build/ios/iphoneos/*.app
    publishing:
      email:
        recipients:
          - aktasemre@gmail.com

  ios-release:
    name: iOS Release Build
    instance_type: mac_mini_m1
    environment:
      vars:
        APP_STORE_APP_ID: YOUR_APP_ID
        GOOGLE_SERVICES_PLIST: ENCRYPTED[!]
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Decode Firebase config
        script: |
          echo $GOOGLE_SERVICES_PLIST | base64 --decode > $CM_BUILD_DIR/ios/Runner/GoogleService-Info.plist
      - name: Set up keychain
        script: keychain initialize
      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files $APP_STORE_APP_ID \
            --type IOS_APP_STORE \
            --certificates-repo $CERTIFICATES_REPO
          keychain add-certificates
      - name: Get packages
        script: flutter pub get
      - name: Recreate iOS Configuration
        script: |
          flutter create . --platforms=ios
      - name: Build release (Flutter will run pod install automatically)
        script: |
          flutter build ios --release
      - name: Create IPA
        script: |
          cd ios
          xcodebuild -workspace Runner.xcworkspace -scheme Runner \
            -configuration Release -sdk iphoneos \
            -archivePath $CM_BUILD_DIR/build/ios/archive/Runner.xcarchive archive
          xcodebuild -exportArchive \
            -archivePath $CM_BUILD_DIR/build/ios/archive/Runner.xcarchive \
            -exportOptionsPlist $CM_BUILD_DIR/ios/export_options.plist \
            -exportPath $CM_BUILD_DIR/build/ios/ipa
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - aktaesemre@gmail.com
      app_store_connect:
        api_key: $APP_STORE_CONNECT_KEY
        key_id: $APP_STORE_CONNECT_KEY_ID
        issuer_id: $APP_CONNECT_ISSUER_ID
        submit_to_testflight: true